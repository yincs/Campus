<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.changs.campus.core.mapper.AccountMapper">
	<insert id="insertOne" parameterType="Account" keyProperty="id" >
		INSERT INTO vip (userName,level) VALUES (#{userName},#{level})
		INSERT INTO account (account,passwd,userId) VALUES (#{account},#{passwd},#{userId})
	</insert>
</mapper>